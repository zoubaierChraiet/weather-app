import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  card: {
    display: "flex !important",
    backgroundColor: "rgba(255, 255, 255, 0.2) !important",
    boxShadow: "0px 0px 10px silver !important",
  },
});
