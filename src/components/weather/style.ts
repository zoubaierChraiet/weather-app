import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  subHeader: { marginBottom: 15 },
  infoWrapper: { marginBottom: 15 },
  icon: { marginRight: 5 },
  typography: { width: '25%' }
});
